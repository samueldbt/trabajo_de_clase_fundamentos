import numpy as np

lista = [1, 2, 3]
lista2 = [3, 2, 1]

l1 = np.array(lista)
l2 = np.array(lista2)

resultado = l1 + l2 # se convierta en la nueva lista resultado de la suma vectorial, que loco, es re poderoso el numpy

print(resultado)
